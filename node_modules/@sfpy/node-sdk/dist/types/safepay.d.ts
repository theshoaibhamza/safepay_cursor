import { Channel, Environment } from '../utils';
export declare type SafepayEnvironment = Environment.Sandbox | Environment.Production | Environment.Development;
export declare type SafepayCurrency = 'PKR' | 'USD' | 'AED' | 'SAR' | 'CAD' | 'EUR' | 'GBP';
export declare type SafepayOptions = {
    environment: SafepayEnvironment;
    apiKey: string;
    v1Secret: string;
    webhookSecret: string;
    channel?: Channel;
};
export declare type SafepayConfig = {
    environment: SafepayEnvironment;
    apiKey: string;
    v1Secret: string;
    webhookSecret: string;
    channel: Channel;
};
