import { Channel, Environment } from './constants';
export declare function buildCheckoutUrl(channel: Channel, env: Environment): string;
export declare function buildApiUrl(env: Environment): string;
